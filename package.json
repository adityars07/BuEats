{
  "name": "bueats-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd foodcampus && npm install && npm run build",
    "install-all": "npm install && cd foodcampus && npm install && cd ../backend && npm install"
  }
}
